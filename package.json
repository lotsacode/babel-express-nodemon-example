{
  "name": "babeltest",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "nodemon": "^1.11.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib",
    "start": "nodemon --watch src --exec 'sudo npm run build && nodejs server.js'"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "author": "",
  "license": "ISC"
}
